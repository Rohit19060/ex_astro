{
  "name": "@example/minimal",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev --host --open --port 3000",
    "start": "astro dev --host --open --port 3000",
    "build": "astro build",
    "preview": "astro preview --host --open --port 3000",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/preact": "^3.0.1",
    "@astrojs/rss": "^4.0.1",
    "astro": "^4.0.6",
    "preact": "^10.19.3"
  }
}